These are real files on the guest's ext4 root filesystem, listed by
mod_autoindex. httpd.conf and nginx.conf are symlinks to the live configs
of the two servers you can start from the console (httpd-demo, nginx-demo).
